# 페어봇 사용설명서

이 문서는 `kis_adaptive_vb_bot`을 처음 설치하는 사람, 이미 실행 중인 봇을 운영하는 사람, 문제가 생겼을 때 빠르게 확인해야 하는 사람을 위한 실무용 안내서다. 가능한 한 "어디를 열어야 하는지", "무엇을 확인해야 하는지", "실거래를 어떻게 안전하게 제어하는지"를 한 문서에서 바로 볼 수 있게 정리했다.

## 1. 봇이 하는 일

페어봇은 한국 ETF 4종을 두 개의 그룹으로 나눠서 관리한다.

- KOSPI 그룹: `122630`, `252670`
- KOSDAQ 그룹: `233740`, `251340`

각 그룹에서 레버리지와 인버스 중 한 방향만 승인하고, 승인된 종목만 목표가 돌파를 감시한다. 한 번 진입한 뒤에는 ATR 기반 트레일링 스탑을 적용하고, 장 마감 전에는 무조건 포지션을 정리한다.

## 2. 설치

### 2.1 기본 준비

```bash
cd ~/Desktop/kis_adaptive_vb_bot
bash scripts/bootstrap.sh
```

이 스크립트는 `venv/` 가상환경을 만들고 `requirements.txt`를 설치한다.

### 2.2 환경 변수 파일 생성

```bash
cp .env.example .env
nano .env
```

반드시 입력해야 하는 값:

```env
KIS_APP_KEY=...
KIS_APP_SECRET=...
KIS_ACCOUNT_NO=12345678
KIS_ACCOUNT_PRODUCT_CODE=01
```

## 3. 실거래 안전장치

실제 주문은 아래 네 가지 조건이 동시에 맞아야만 전송된다.

```env
KIS_LIVE_ENABLED=1
KIS_LIVE_CONFIRM=YES
KIS_KILL_SWITCH=0
```

그리고 프로젝트 루트에 `STOP_TRADING.flag` 파일이 없어야 한다.

주문을 강제로 막고 싶을 때:

```bash
cd ~/Desktop/kis_adaptive_vb_bot
touch STOP_TRADING.flag
```

다시 주문을 허용하고 싶을 때:

```bash
cd ~/Desktop/kis_adaptive_vb_bot
rm -f STOP_TRADING.flag
```

권장 순서는 아래와 같다.

1. 처음에는 `KIS_LIVE_ENABLED=0`, `STOP_TRADING.flag` 유지
2. 로그와 리포트가 정상 생성되는지 확인
3. 필요한 경우 웹소켓 점검
4. 마지막에만 `KIS_LIVE_ENABLED=1`과 `STOP_TRADING.flag` 해제

## 4. 실행과 중지

### 4.1 실행

```bash
bash ~/Desktop/kis_adaptive_vb_bot/scripts/run_bot.sh
```

### 4.2 중지

```bash
bash ~/Desktop/kis_adaptive_vb_bot/scripts/stop_bot.sh
```

### 4.3 웹소켓 점검

```bash
bash ~/Desktop/kis_adaptive_vb_bot/scripts/ws_smoke_test.sh
```

## 5. 장중 타임라인

- 08:50: 일봉과 지표 갱신
- 09:01: 각 그룹 방향 결정
- 09:00~13:00: 목표가 돌파 감시 및 진입
- 진입 후: 샹들리에 트레일링 스탑 감시
- 15:15: 남은 포지션 전량 청산
- 종료 후: 리포트 및 요약 생성

장 마감 뒤에 수동 실행하면, 이미 청산 시간이 지난 상태라 빠르게 종료될 수 있다. 이건 이상이 아니라 시간 규칙이 정상 동작한 결과일 수 있다.

## 6. 로그와 리포트 읽는 법

### 6.1 로그

```bash
tail -n 200 ~/Desktop/kis_adaptive_vb_bot/logs/nohup_adaptive_vb.log
tail -n 200 ~/Desktop/kis_adaptive_vb_bot/logs/pairbot_$(date +%F).log
```

`nohup_adaptive_vb.log`는 실행 전체 흐름을 넓게 볼 때 좋고, 날짜별 로그는 특정 거래일을 집중해서 볼 때 편하다.

### 6.2 일일 리포트

```bash
sed -n '1,220p' ~/Desktop/kis_adaptive_vb_bot/reports/$(date +%F)/report.md
tail -n 50 ~/Desktop/kis_adaptive_vb_bot/reports/$(date +%F)/events.jsonl
column -s, -t < ~/Desktop/kis_adaptive_vb_bot/reports/$(date +%F)/trades.csv | head
```

리포트에서 가장 먼저 볼 항목:

- 어떤 그룹이 승인되었는지
- 목표가가 계산됐는지
- 실제 진입이 있었는지
- 청산 사유가 무엇인지
- 오류가 발생했는지

### 6.3 체결가와 손익 해석

이 프로젝트는 주문 API 응답 특성상 정확한 체결가를 항상 받을 수 없다. 그래서 `trades.csv`에는 주문 직전 관측 가격이 가정 체결가로 들어갈 수 있다. SELL 행의 `pnl_krw`도 운영 참고용 추정치로 보고, 최종 정산은 증권사 체결내역과 대조해야 한다.

## 7. 자주 막히는 문제

### 7.1 주문이 전혀 나가지 않을 때

아래 순서대로 확인한다.

1. `.env`에 `KIS_LIVE_ENABLED=1`인가
2. `.env`에 `KIS_LIVE_CONFIRM=YES`인가
3. `.env`에 `KIS_KILL_SWITCH=0`인가
4. `STOP_TRADING.flag`가 남아 있지 않은가

### 7.2 휴장일인데 실행되는 것처럼 보일 때

이 봇은 `utils_holiday.py`를 통해 주말과 휴장일을 자동으로 건너뛴다. 로그에 `market_closed` 이벤트가 남아 있으면 정상 동작이다.

### 7.3 웹소켓이 불안정할 때

웹소켓이 실패해도 REST 폴링으로 폴백하도록 설계되어 있다. 다만 장중 지연이 커질 수 있으므로, 안정적인 실시간 감시가 필요하면 `scripts/ws_smoke_test.sh`로 먼저 품질을 확인하는 편이 좋다.

## 8. 운영 체크리스트

- [ ] `.env`가 올바르게 작성됨
- [ ] `STOP_TRADING.flag` 상태를 의도대로 확인함
- [ ] 실거래 스위치를 무심코 켜지 않았는지 점검함
- [ ] 당일 로그 파일이 생성되는지 확인함
- [ ] `reports/YYYY-MM-DD/` 아래 리포트가 생성되는지 확인함
- [ ] 장 마감 후 최종 손익은 증권사 체결 내역으로 대조함

## 9. 공개 저장소 주의사항

이 저장소를 공개로 운영할 때는 아래 파일을 절대 커밋하지 않는다.

- `.env`
- `.kis_token_cache.json`
- `.kis_approval_cache.json`
- `logs/`
- PID, lock 성격 파일

보고서 예시를 올릴 때도 계좌 식별 정보나 민감 로그가 없는지 확인해야 한다.
